{
  "_test_intent": "update that renames a Cloud Run service AWAY from a protected name — before-check must still fire control-plane-service",
  "format_version": "1.2",
  "resource_changes": [
    {
      "address": "google_cloud_run_v2_service.agent",
      "mode": "managed",
      "type": "google_cloud_run_v2_service",
      "name": "agent",
      "change": {
        "actions": ["update"],
        "before": {"name": "driftscribe-agent"},
        "after": {"name": "something-else"}
      }
    }
  ]
}
