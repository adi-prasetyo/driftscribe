{
  "_test_intent": "update of the approval-hmac-key secret — must emit control-plane-secret",
  "format_version": "1.2",
  "resource_changes": [
    {
      "address": "google_secret_manager_secret.hmac",
      "mode": "managed",
      "type": "google_secret_manager_secret",
      "name": "hmac",
      "change": {
        "actions": ["update"],
        "before": {"secret_id": "approval-hmac-key"},
        "after": {"secret_id": "approval-hmac-key"}
      }
    }
  ]
}
