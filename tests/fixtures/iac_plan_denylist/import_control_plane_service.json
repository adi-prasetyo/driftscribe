{
  "_test_intent": "import of the coordinator Cloud Run service — must emit exactly control-plane-service (type is adoptable; the §4.1 identity rule still fires)",
  "format_version": "1.2",
  "resource_changes": [
    {
      "address": "google_cloud_run_v2_service.agent",
      "mode": "managed",
      "type": "google_cloud_run_v2_service",
      "name": "agent",
      "change": {
        "actions": ["no-op"],
        "before": {"name": "driftscribe-agent"},
        "after": {"name": "driftscribe-agent"},
        "importing": {"id": "driftscribe-agent"}
      }
    }
  ]
}
