{
  "_test_intent": "delete of a Cloud Run service identified only via before.name (after is null) — must emit BOTH control-plane-service AND delete-action-forbidden-v1",
  "format_version": "1.2",
  "resource_changes": [
    {
      "address": "google_cloud_run_v2_service.agent",
      "mode": "managed",
      "type": "google_cloud_run_v2_service",
      "name": "agent",
      "change": {
        "actions": ["delete"],
        "before": {"name": "driftscribe-agent"},
        "after": null
      }
    }
  ]
}
