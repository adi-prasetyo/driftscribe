{
  "_test_intent": "create of a google_secret_manager_secret_version whose parent path resolves to approval-hmac-key — must emit control-plane-secret",
  "format_version": "1.2",
  "resource_changes": [
    {
      "address": "google_secret_manager_secret_version.hmac_v2",
      "mode": "managed",
      "type": "google_secret_manager_secret_version",
      "name": "hmac_v2",
      "change": {
        "actions": ["create"],
        "before": null,
        "after": {
          "secret": "projects/driftscribe-hack-2026/secrets/approval-hmac-key",
          "enabled": true
        }
      }
    }
  ]
}
