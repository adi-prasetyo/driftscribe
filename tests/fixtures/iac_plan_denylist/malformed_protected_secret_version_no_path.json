{
  "_test_intent": "google_secret_manager_secret_version create whose after.name lacks /secrets/<id>/ in the resource path — defensive bias-to-deny via plan-json-malformed-change",
  "format_version": "1.2",
  "resource_changes": [
    {
      "address": "google_secret_manager_secret_version.broken",
      "mode": "managed",
      "type": "google_secret_manager_secret_version",
      "name": "broken",
      "change": {
        "actions": ["create"],
        "before": null,
        "after": {"name": "something-without-/the/segment-in-it"}
      }
    }
  ]
}
