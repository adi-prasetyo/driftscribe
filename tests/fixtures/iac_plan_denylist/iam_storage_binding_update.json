{
  "_test_intent": "update of a google_storage_bucket_iam_member (substring _iam_ in rtype) — must emit iam-change-forbidden-v1",
  "format_version": "1.2",
  "resource_changes": [
    {
      "address": "google_storage_bucket_iam_member.uploads_writer",
      "mode": "managed",
      "type": "google_storage_bucket_iam_member",
      "name": "uploads_writer",
      "change": {
        "actions": ["update"],
        "before": {"bucket": "payment-demo-uploads", "role": "roles/storage.objectViewer"},
        "after": {"bucket": "payment-demo-uploads", "role": "roles/storage.objectAdmin"}
      }
    }
  ]
}
