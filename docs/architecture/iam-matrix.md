# DriftScribe IAM matrix

> **Status:** Phase 17 complete — multi-agent framework + Developer Knowledge MCP. Two workloads (drift, upgrade) share the coordinator + notifier and each have their own worker pair (drift: reader/docs/rollback; upgrade: upgrade-reader/upgrade-docs). The coordinator runs under a dedicated `driftscribe-agent@…` SA with per-secret bindings on `coordinator-shared-token`, `github-pat`, and `developer-knowledge-api-key`. The bootstrap script (`infra/scripts/setup_secrets.sh`) creates all 7 SAs, applies per-SA IAM (including the resource-scoped rollback grant on `payment-demo` only, the project-wide `roles/aiplatform.user` on the coordinator for Vertex AI, and per-worker `run.invoker` grants for the coordinator on all three workloads' worker sets — drift, upgrade, and the read-only explore/`driftscribe-infra-reader`), and is idempotent. (`infra-reader-sa` itself is created operator-side per [`docs/runbooks/infra-reader.md`](../runbooks/infra-reader.md), not by this script — the script only adds the coordinator's `run.invoker` on the infra-reader once it's deployed.) The operator runbook is [`docs/runbooks/deploy.md`](../runbooks/deploy.md).

This document is the source of truth for **what each service account can do, and what it explicitly cannot do**. The "negative-space" column is load-bearing — it's not enough to enumerate the grants; reviewers and judges should be able to run `gcloud projects get-iam-policy driftscribe-hack-2026 --format=json` and verify nothing beyond the listed bindings is present.

## Matrix

| Service Account | Cloud Run service it backs | Workload scope | IAM bindings (positive) | Negative-space (explicit non-grants) | Phase |
| --- | --- | --- | --- | --- | --- |
| `driftscribe-agent@…` (coordinator) | `driftscribe-agent` | all (dispatcher; routes `workload=drift`, `workload=upgrade`, and read-only `workload=explore`) | `roles/run.invoker` on each worker service (per-service binding, not project-wide) — drift side: `driftscribe-reader`, `driftscribe-docs`, `driftscribe-rollback`, `driftscribe-notifier`; upgrade side: `driftscribe-upgrade-reader`, `driftscribe-upgrade-docs` (notifier is shared, already in the drift list); explore side: `driftscribe-infra-reader` (the read-only Phase infra-iac B whole-project inventory worker); provision side: `driftscribe-tofu-editor` (the Phase D HCL-authoring worker the coordinator invokes to open `infra/` PRs — write-via-PR only, never an infra mutator). `roles/secretmanager.secretAccessor` on the *specific named secrets* the coordinator needs: `coordinator-shared-token`, `github-pat` (PAT for `search_recent_prs_tool` AND, since Phase C5e, for **merging approved IaC PRs** — see negative-space column for the now-required write scope), and `developer-knowledge-api-key` (Phase 17.B.1 — the Google Developer Knowledge MCP API key; coordinator-only, no worker holds this). `roles/datastore.user` **CONDITIONED to the `(default)` Firestore database** (Phase C5f — `resource.name == "projects/driftscribe-hack-2026/databases/(default)"`; the coordinator writes to `sessions/` for the state store and READS `approvals/` to render the operator-facing approval page — the `pending → denied` flip itself moved to the rollback worker in Phase 11.9, see [the carry-over note](#phase-119-carry-overs-from-codex-11-7-review)). The condition **denies** the separate named `plan-approvals` database (only `tofu-apply-sa` reaches that DB, conditioned the other way), so a compromised coordinator cannot read or write plan-approval records. Firestore IAM is database-level, so this condition is enforced by the REST/client libraries (the Cloud Console does NOT honour IAM conditions); the worker selects the DB via env `PLAN_APPROVALS_DB` (empty ⇒ `(default)` for back-compat); `roles/aiplatform.user` (project-wide — Phase 14.5; required so the coordinator's runtime ADC can call Vertex AI's `generate-content` endpoint for `gemini-2.5-flash`, the model the ADK path drives). **Note:** the coordinator does NOT hold `approval-hmac-key` — only the rollback worker does, which is what makes the approve/deny authority split meaningful (a compromised coordinator can refuse executions but cannot mint OR silently deny them after Phase 11.9). After Phase 14.5 the coordinator additionally holds **NO external LLM API key** of any kind — Gemini auth flows entirely through Vertex AI ADC bound to this SA, so leaked-key revocation is a no-op (there is no key to leak). The Developer Knowledge MCP key IS held by the coordinator (Phase 17.B.1) because MCP is an attached tool at the reasoning layer; no worker has MCP access. | **NOT** `roles/run.developer` (cannot deploy/modify Cloud Run — Phase 11.7 delegated this entirely to the rollback worker, resource-scoped to `payment-demo`); **NOT** `roles/run.viewer` (Phase 13 removed this — both `/recheck` paths now route live-state reads through the Reader Worker, so the coordinator cannot read any service's env directly); **NOT** `roles/secretmanager.secretAccessor` at project scope (only the named secrets above); **NOT** `roles/iam.serviceAccountTokenCreator` (cannot impersonate any other SA); **NOT** GitHub admin scope. The coordinator's `github-pat` MUST be a **fine-grained PAT** scoped to `adi-prasetyo/driftscribe` **only** (NO admin, NO second repo such as `driftscribe-e2e-target`). Since Phase C5e the coordinator legitimately **merges approved IaC PRs**, so the required scope is: `Contents: Read and write` (the PR-merge endpoint needs `Contents: write`), `Pull requests: Read` (get PR / head sha / state), and `Checks: Read` (the readiness gate — the coordinator reads **check-runs** only via `get_check_runs`, NOT combined commit statuses; any required commit-status checks are enforced server-side by GitHub branch protection at merge, so no `Commit statuses` scope is needed). Nothing else — no Issues, no Actions/workflow, no admin, no 2nd repo. (The post-merge audit comment via `pr.create_issue_comment` is **best-effort** and may 403 under this minimal scope without losing the merge; add `Pull requests: write` only if the comment is wanted.) The LIVE `github-pat` secret is still an **over-scoped classic PAT** (repo/workflow/admin/push on both `adi-prasetyo/driftscribe` and `driftscribe-e2e-target`) pending rotation to the above fine-grained PAT in Phase C5f — until rotation the single-repo / no-write / no-admin negative space is weakened. The drift docs worker holds a *separate* fine-grained PAT for `Contents: write` + `Pull requests: write`. The upgrade-reader worker holds another *separate* read-only fine-grained PAT (`upgrade-reader-github-pat`). The upgrade-docs worker holds yet another *separate* read+write fine-grained PAT (`upgrade-docs-github-pat`). The tofu-editor worker (Phase D) holds yet another *separate* read+write fine-grained PAT (`tofu-editor-github-pat`) for authoring `infra/` PRs. Five distinct PATs, one per role, none escalating. (Historically the coordinator's PAT was read-only; Phase C5e added the legitimate IaC-PR merge, so the negative space is now "single-repo, no admin, no second repo, no write beyond `Contents`/`Pull requests`" rather than "no write at all" — and that tightened negative space only holds once C5f rotates the LIVE over-scoped classic PAT.) **NOT** the rollback HMAC key. **NOT** an AI Studio Gemini API key (Phase 14.5 — removed from `--set-secrets` and from `Settings`; the `gemini-api-key` Secret Manager resource is orphaned post-14.5 and operator-deleted manually). **NOT** `roles/run.invoker` on any service outside the eight worker services listed in positive grants. The coordinator's `roles/run.invoker` grants are per-service, not project-wide, and **explicitly do NOT extend across workloads** (drift, upgrade, and the read-only explore/infra-reader set) — Phase 17.D.5 makes this explicit: a Cloud Run service is granted `run.invoker` to the coordinator only after that worker's SA exists AND the worker has been deployed; the script's two-pass design surfaces a missing grant as a clean 403 at the Cloud Run admission layer rather than as a silent application-layer failure. The workload-scoped invariant is enforced by the test in `tests/integration/test_upgrade_deploy_pin.py` (Phase 17.C.5): the worker-side env pin (`UPGRADE_TARGET_REPO`) must agree with `UPGRADE_TARGET_REGISTRY["phase17_demo"].target_repo`, and the worker's `_LOCKFILE_PATH_RE` must accept the registry's `lockfile_path`. | 8 (initial); IAM trimmed in 11.7; further doc-corrections in 11.9; 13 (run.viewer removed); 14.5 (Vertex AI ADC replaces AI Studio API key); 17.B.1 (Developer Knowledge MCP API key added); 17.D.5 (upgrade workers added); C5e (coordinator merges IaC PRs — github-pat now needs write); C5f (datastore.user DB-conditioned; github-pat fine-grained rotation) |
| `reader-agent-sa@…` | `driftscribe-reader` | drift | `roles/run.viewer` on the project (lets it call Cloud Run admin to read service env + revision lists) | **NOT** `roles/run.developer` (cannot deploy or modify any service); **NOT** `roles/iam.serviceAccountTokenCreator`; **NOT** any Secret Manager access; **NOT** any GitHub credentials | 11.3 |
| `docs-agent-sa@…` | `driftscribe-docs` | drift | `roles/secretmanager.secretAccessor` on **one** secret only: `docs-agent-github-pat` (per-secret binding); `roles/run.invoker` granted *to the coordinator on this service* (per-service binding, set after first deploy) | **NOT** any project-level GCP role; **NOT** able to read or write Cloud Run state; **NOT** Firestore; **NOT** any other secret; **NOT** any GCP-level GitHub credential. The GitHub PAT injected as `GITHUB_TOKEN` is fine-grained: `Contents: Read & write` + `Pull requests: Read & write` on `adi-prasetyo/driftscribe` *only* — no org admin, no other repos, no account-level scopes. The worker's Layer 2 policy additionally hardcodes the target repo via env (caller cannot override), enforces a `^demo/docs/[^/]+\.md$` path allowlist with `normpath`-based traversal defense, refuses hidden files, requires the branch to start with `driftscribe/`, and refuses any base other than `main` | 11.4 |
| `rollback-agent-sa@…` | `driftscribe-rollback` | drift | `roles/run.developer` **on `payment-demo` service only** via a resource-scoped binding (`gcloud run services add-iam-policy-binding payment-demo --member=… --role=roles/run.developer`); `roles/iam.serviceAccountUser` (`actAs`) **on the dedicated `payment-demo-runtime@…` runtime SA only** (Phase C5f, `setup_secrets.sh` §7b — Cloud Run requires the caller to `actAs` the service's runtime SA for ANY update, so the `/execute` traffic-shift `update_service` call needs this; the C4 plan named only `tofu-apply-sa` and this rollback grant was the gap C5f closes); `roles/datastore.user` **CONDITIONED to the `(default)` Firestore database** (Phase C5f — `resource.name == "projects/driftscribe-hack-2026/databases/(default)"`; same [acknowledged constraint](#acknowledged-constraints) as coordinator re: collection-scope IAM, but the database-level condition **denies** the separate named `plan-approvals` database reached only by `tofu-apply-sa`); `roles/secretmanager.secretAccessor` on `approval-hmac-key` only; `roles/run.invoker` granted *to the coordinator on this service* (per-service binding, set after first deploy). The worker's Layer 2 policy hardcodes `TARGET_SERVICE=payment-demo` via env, refuses any caller-supplied service field (`extra="forbid"` schema), refuses rollback targets that aren't in the service's revision list, and refuses rollback targets equal to the currently-active revision. Layer 4 enforces a single-use HMAC-bound approval token with a 15-min TTL and transactional `pending→used` flip — see [`driftscribe_lib/approvals.py`](../../driftscribe_lib/approvals.py) | **NOT** project-wide `roles/run.developer` (cannot touch the coordinator, the reader, the docs worker, or itself); **NOT** any other Cloud Run service (resource-scoped binding only on `payment-demo`); **NOT** any other Secret Manager secret; **NOT** GitHub access of any kind; **NOT** `roles/iam.serviceAccountTokenCreator`; **NOT** `actAs` on any SA other than `payment-demo-runtime@…` (the C5f `serviceAccountUser` grant is resource-scoped to that one runtime SA — it cannot impersonate `tofu-apply-sa`, the coordinator, or any default/compute SA); **NOT** the named `plan-approvals` Firestore database (its `datastore.user` is conditioned to `(default)` only) | 11.5; runtime-SA `actAs` + DB-conditioned datastore added C5f |
| `notifier-agent-sa@…` | `driftscribe-notifier` | both (shared across workloads) | `roles/secretmanager.secretAccessor` on **one** secret only: `driftscribe-webhook-url` (per-secret binding); `roles/run.invoker` granted *to the coordinator on this service* (per-service binding, set after first deploy). The worker's Layer 2 policy hardcodes the outbound URL via Secret-Manager-injected env (`NOTIFY_WEBHOOK_URL`), refuses any caller-supplied `url` field via `extra="forbid"`, constrains `channel` to `info\|alert\|approval` and `severity` to `low\|medium\|high\|critical` via `Literal`, and caps `body` at 10000 chars. Fail-closed: empty / missing webhook URL at boot raises `RuntimeError` and the revision fails to come up | **NOT** any project-level GCP role grants whatsoever. **NOT** `roles/run.viewer` (cannot read Cloud Run state); **NOT** `roles/run.developer` (cannot deploy or modify any service); **NOT** `roles/datastore.user` (cannot read or write Firestore); **NOT** any other Secret Manager secret (per-secret binding scoped to `driftscribe-webhook-url` only); **NOT** any GitHub credentials; **NOT** `roles/iam.serviceAccountTokenCreator`. The notifier's "capability" *is* the webhook URL — knowing the URL is the entire authorization model. The notifier is shared across both workloads but the workload abstraction is invisible to it — both drift and upgrade flows call `/notify` with the same closed schema | 11.6 |
| `upgrade-reader-sa@…` | `driftscribe-upgrade-reader` | upgrade | `roles/secretmanager.secretAccessor` on **one** secret only: `upgrade-reader-github-pat` (per-secret binding); `roles/run.invoker` granted *to the coordinator on this service* (per-service binding, set after first deploy). The worker's Layer 2 policy: `UPGRADE_TARGET_REPO` env-pinned at boot and re-validated against request body's `target_repo` BEFORE any GitHub call; `lockfile_path` from request body must match `re.fullmatch(r"demo/upgrade-target/package\.json\Z")` AND survive a dual traversal guard (`".." in segments` OR `normpath` mismatch) — both pre-regex; advisory source hardcoded to GitHub Advisory DB (URL is a module-level constant, no caller-supplied URLs). Worker isolation invariant: MUST NOT import `agent.workloads.registry` or any other `agent.*` module — enforced by `tests/integration/test_upgrade_e2e.py::test_worker_does_not_import_coordinator_registry` which spawns a fresh interpreter and inspects `sys.modules` | **NOT** any project-level GCP role grants whatsoever. **NOT** `roles/run.viewer` / `roles/run.developer` / `roles/datastore.user`. **NOT** any other Secret Manager secret. **NOT** GitHub write access of any kind — the PAT is fine-grained, scoped to `adi-prasetyo/driftscribe` *only*, with `Contents: Read` + `Pull requests: Read` (no write). **NOT** `roles/iam.serviceAccountTokenCreator`. **NOT** access to `developer-knowledge-api-key` (MCP is coordinator-only — workers have no MCP attach). **NOT** access to `upgrade-docs-github-pat` (the docs worker's write PAT is bound to a different SA). The coordinator's `roles/run.invoker` on this service does NOT extend to any drift worker (workload-scoped invariant). | 17.C.2 |
| `upgrade-docs-sa@…` | `driftscribe-upgrade-docs` | upgrade | `roles/secretmanager.secretAccessor` on **one** secret only: `upgrade-docs-github-pat` (per-secret binding); `roles/run.invoker` granted *to the coordinator on this service* (per-service binding, set after first deploy). The worker's Layer 2 policy: `UPGRADE_TARGET_REPO` env-pinned + re-validated (same as upgrade-reader); `lockfile_path` regex + dual traversal guard (identical regex to upgrade-reader, pinned by `tests/integration/test_upgrade_deploy_pin.py::test_upgrade_reader_and_docs_use_identical_lockfile_regex`); `branch` must start with `upgrade/` and tail-match `[A-Za-z0-9._/-]{1,200}\Z` (rejects whitespace, control chars, `..`); `base` must be `"main"`; `title` must start with `"upgrade"`. Mutation surface is strictly `dependencies[package_name] = target_version`; every other lockfile key is preserved byte-identically modulo JSON re-serialization. Layer 3 (post-LLM deterministic validator, Phase 17.C.3a in `workers/upgrade_docs/validator.py`) runs AFTER the lockfile read and BEFORE the GitHub write: lockfile-path regex re-check, package_name must exist in current lockfile, target_version > current (no downgrades), version_jump ∈ {patch, minor} (major bumps refused with 403 → escalation), `advisory_url` must match `re.fullmatch(r"https://github\.com/advisories/GHSA-[a-zA-Z0-9-]+$")`. Worker isolation invariant identical to upgrade-reader. | **NOT** any project-level GCP role grants whatsoever. **NOT** `roles/run.viewer` / `roles/run.developer` / `roles/datastore.user`. **NOT** any other Secret Manager secret. **NOT** GitHub admin scope, account-level scope, or any other repo — the PAT is fine-grained, scoped to `adi-prasetyo/driftscribe` *only*, with `Contents: Read & write` + `Pull requests: Read & write` (no Issues, no Actions). **NOT** `roles/iam.serviceAccountTokenCreator`. **NOT** access to `developer-knowledge-api-key` (MCP is coordinator-only). **NOT** access to `upgrade-reader-github-pat` (the reader worker's read-only PAT is bound to a different SA — defense in depth even though the docs worker's write PAT supersets the reader's). The coordinator's `roles/run.invoker` on this service does NOT extend to any drift worker (workload-scoped invariant). | 17.C.3 / 17.C.3a |
| `infra-reader-sa@…` | `driftscribe-infra-reader` | explore (read-only whole-project inventory; Phase infra-iac B) | The custom role `projects/<project>/roles/driftscribeInfraReader` — **exactly** two permissions, `cloudasset.assets.searchAllResources` + `serviceusage.services.use` (the recommended least-privilege grant); OR, during migration, the broader predefined pair `roles/cloudasset.viewer` + `roles/serviceusage.serviceUsageConsumer` (what the first Phase B deploy granted live — see the reconciliation note in `docs/runbooks/infra-reader.md` §2). `roles/run.invoker` granted *to the coordinator on this service* (per-service binding, set after first deploy by `infra/scripts/setup_secrets.sh` — workers are `--no-allow-unauthenticated`, so this platform grant is required IN ADDITION to the in-app `ALLOWED_CALLERS` allowlist). The worker reads CAI `searchAllResources` with a minimal read_mask (name/asset_type/location) and labels each resource declared-in-IaC vs not by parsing the `iac/*.tf` baked into its own image. | **NOT** any write/mutation role of any kind — **NOT** `roles/run.developer`, **NOT** `roles/run.viewer`, **NOT** `roles/editor`/`owner`. **NOT** any Secret Manager secret — it never reads secret *contents*; Secret Manager resources surface in the inventory as **counts-only** metadata. **NOT** any OpenTofu **state bucket** or **KMS** access — it parses HCL baked into its image, never reads tofu state and never decrypts anything (this is what keeps it strictly read-only and outside the apply trust boundary). **NOT** any GitHub credential. **NOT** `roles/iam.serviceAccountTokenCreator`. Its entire effective capability is *read-only resource metadata via CAI* — the documented scoped exception in `docs/runbooks/infra-reader.md`. The coordinator's `roles/run.invoker` on this service does NOT extend to any drift or upgrade worker (workload-scoped invariant). | infra-iac B |
| `tofu-apply-sa@…` | `driftscribe-tofu-apply` | apply (the **sole mutator** — the only SA that runs `tofu apply` against project infra; Phase C4/C5f) | `roles/run.developer` **project-wide** (hardened-broad — the apply worker must be able to update Cloud Run services declared in IaC; explicitly **NO** `setIamPolicy`); `roles/storage.objectAdmin` on the **tofu state bucket** only (read/write/lock OpenTofu state); `roles/cloudkms.cryptoKeyEncrypterDecrypter` on the **tofu-state KMS key** only (state encryption/decryption); `roles/storage.objectViewer` on the **artifact bucket** only (read the trusted plan triplet C2 produced); `roles/datastore.user` **CONDITIONED to the named `plan-approvals` Firestore database** (Phase C5f — `resource.name == "projects/driftscribe-hack-2026/databases/plan-approvals"`; the apply worker reads/burns the plan-bound approval record, which is isolated into this separate named DB in `asia-northeast1`); `roles/iam.serviceAccountUser` (`actAs`) **on `payment-demo-runtime@…` only** (Phase C5f, `setup_secrets.sh` §7b — Cloud Run requires `actAs` on the service's runtime SA for ANY update, including the C5g service-side repoint of `payment-demo` to the dedicated runtime SA, applied in-place through this pipeline). All from `infra/scripts/setup_iac_backend.sh` §6.5. | **NOT** any `*.setIamPolicy` permission on any resource (cannot rewrite IAM — `run.developer` here is the variant without `run.services.setIamPolicy`); **NOT** project-wide `actAs` / `roles/iam.serviceAccountUser` at project scope (the only `actAs` is resource-scoped to `payment-demo-runtime@…`); **NOT** `roles/iam.serviceAccountTokenCreator` (cannot mint tokens for other SAs); **NOT** any KMS **key creation/destruction** role (`cryptoKeyEncrypterDecrypter` only — encrypt/decrypt, no `cloudkms.cryptoKeys.create`); **NOT** `storage.objectAdmin` on any bucket other than the state bucket (artifact bucket is **read-only** `objectViewer`); **NOT** the `(default)` Firestore database (its `datastore.user` is conditioned to the named `plan-approvals` DB only — it cannot touch `sessions/` or `approvals/`) — **but this holds in the DEFAULT hardened IAM mode only**; the opt-in `TOFU_APPLY_IAM_MODE=editor` fast path grants `roles/editor`, which carries UNCONDITIONED all-database Firestore data access and therefore FORFEITS the apply SA's named-DB fencing (the script prints a warning; the coordinator's `(default)`-fencing is unaffected since it never runs in editor mode); **NOT** any GitHub credential; **NOT** any Secret Manager secret *contents* beyond what the pipeline injects. The C2 plan-builder is trusted-but-unprivileged (it cannot apply); this SA is the single trust boundary where IaC mutations actually land, gated by the plan-bound HMAC approval it verifies before applying. | C4 / C5f |
| `tofu-editor-sa@…` | `driftscribe-tofu-editor` | provision (the HCL **authoring** worker — opens `infra/` PRs; Phase D / D3-1) | `roles/secretmanager.secretAccessor` on **one** secret only: `tofu-editor-github-pat` (per-secret binding); `roles/run.invoker` granted *to the coordinator on this service* (per-service binding, set after first deploy by `infra/scripts/setup_secrets.sh` — workers are `--no-allow-unauthenticated`, so this platform grant is required IN ADDITION to the in-app `ALLOWED_CALLERS` allowlist naming the coordinator SA). The worker's only authority is its GitHub PAT: it authors HCL **text** and opens a PR; it never runs `tofu` and never touches GCP infra. It **re-validates every request** before writing — an `iac/`-only (and non-secret-baring) file gate plus the shared AGENT-mode static gate (`tools/iac_static_gate.py`, including the secret-literal ban) — so even a compromised editor cannot author a control-plane / IAM / secret-material change that would later apply. | **NOT** any project-level GCP role of any kind — **NOT** `roles/run.developer`, **NOT** `roles/run.viewer`, **NOT** `roles/datastore.user`, **NOT** `roles/editor`/`owner`. **NOT** any apply/state/KMS access — it holds **NO** OpenTofu **state bucket** grant, **NO** `tofu-state` **KMS** key, **NO** `plan-hmac-key` (it is OUTSIDE the apply trust boundary — `tofu apply` lives only in `tofu-apply-sa`, the sole mutator). **NOT** any other Secret Manager secret — the per-secret binding is scoped to `tofu-editor-github-pat` ONLY (it cannot read `docs-agent-github-pat`, `upgrade-docs-github-pat`, the coordinator's `github-pat`, the HMAC keys, or any other secret). **NOT** `roles/iam.serviceAccountTokenCreator`. The GitHub PAT injected as `GITHUB_TOKEN` is a **fine-grained PAT** scoped to `adi-prasetyo/driftscribe` **only**, with `Contents: Read & write` + `Pull requests: Read & write` and **nothing else** — no Issues, no Actions/workflow, no admin, no account-level scope, no second repo. It is **DISTINCT** from the coordinator's `github-pat`, from `tofu-apply` (which holds no GitHub credential at all), and from the docs / upgrade-docs PATs — **five** separate GitHub PATs total, one per role, none escalating. The coordinator's `roles/run.invoker` on this service does NOT extend to any drift, upgrade, or apply worker (workload-scoped invariant). | D3-1 (Phase D) |
| `eventarc-trigger-sa@…` | *(not backing a Cloud Run service — Eventarc-internal identity)* | drift (trigger filter is hardcoded to `payment-demo`) | `roles/run.invoker` on the `driftscribe-agent` service (per-service binding) — required for Eventarc to validate the trigger destination at create time. The application path doesn't depend on the IAM check (the coordinator is `--allow-unauthenticated` at the Cloud Run admission layer); enforcement happens in app code, which verifies the bearer ID token's `email` claim equals this SA's address per `docs/architecture/eventarc-payload.md` §"Filtering at the handler". `roles/eventarc.eventReceiver` project-wide — required by Eventarc to deliver events to a destination using this SA's identity. **Two** triggers feed `/eventarc` (an audit-log trigger filters exactly one `methodName`), both `serviceName=run.googleapis.com`: `driftscribe-cloudrun-changes` = v1 `google.cloud.run.v1.Services.ReplaceService` + `resourceName=namespaces/<P>/services/payment-demo` — this is the live drift path (gcloud / CI / the demo's `gcloud run services update`); and `driftscribe-cloudrun-changes-v2-update` = v2 `google.cloud.run.v2.Services.UpdateService` + `resourceName=projects/<P>/locations/asia-northeast1/services/payment-demo` (rollback worker / Console / newer clients). The original v2-only trigger was silently dead because this env emits v1. The handler additionally guards on `service_name == "payment-demo"` and `location == "asia-northeast1"` from the audit-log body, so even a broadened trigger filter cannot bypass the in-app service whitelist | **NOT** `roles/run.developer` or any other Cloud Run admin role (cannot read, deploy, or mutate any service — it can only deliver events to `driftscribe-agent`); **NOT** any other Cloud Run service in `run.invoker` (per-service binding is on `driftscribe-agent` only); **NOT** any Secret Manager access; **NOT** `roles/datastore.user` (cannot read or write Firestore directly — the `/eventarc` handler's identity is the *coordinator* SA, not this one); **NOT** `roles/iam.serviceAccountTokenCreator`; **NOT** any GitHub credentials. The SA's effective capability is precisely "receive Eventarc events and deliver them to `/eventarc` on the coordinator"; everything Layer-2 downstream of that runs as the coordinator's SA | 14.3 |

## Why negative space matters

A traditional IAM doc says "here's what each SA can do" and stops. That's how you end up with a service account that ostensibly only needs to read one secret but also happens to have `roles/owner` because someone bound it during a debugging session and forgot to undo it. The negative-space column makes the audit binary: either the policy matches the matrix, or it doesn't.

For the demo, the judge-facing artifact is `infra/scripts/audit_iam.sh` (Phase 11.8, TODO), which:

1. Lists every binding for every DriftScribe SA via `gcloud projects get-iam-policy` and `gcloud run services get-iam-policy`.
2. Diffs against the matrix above.
3. Exits non-zero on any unexpected binding (both extra bindings on the listed SAs and bindings to any non-listed principal that touches our resources).

That script is the architectural enforcement; this document is its specification.

## Per-secret IAM examples

The matrix above invokes "per-secret binding" repeatedly. The exact gcloud invocation is:

```bash
gcloud secrets add-iam-policy-binding coordinator-shared-token \
  --project=driftscribe-hack-2026 \
  --member=serviceAccount:driftscribe-agent@driftscribe-hack-2026.iam.gserviceaccount.com \
  --role=roles/secretmanager.secretAccessor
```

> **Phase ordering note:** the `coordinator-shared-token` secret resource AND this binding to the coordinator SA must both exist *before* the Phase 11.1 deploy can succeed — Cloud Run validates `--set-secrets=DRIFTSCRIBE_TOKEN=coordinator-shared-token:latest` at deploy time and rejects the revision with `INVALID_ARGUMENT` if the resource is missing or unreadable by the runtime SA. The other per-worker bindings in this matrix (reader/docs/rollback/notifier) get added incrementally during Phase 11.3–11.6 and are not a prerequisite for the 11.1 deploy.

The crucial property: **no SA has `roles/secretmanager.secretAccessor` at the project level.** Each grant is scoped to a single secret resource, so a future "let me just read another secret real quick" attempt fails closed.

## Resource-scoped Cloud Run example

The rollback SA's most powerful grant is `roles/run.developer` on `payment-demo` only:

```bash
gcloud run services add-iam-policy-binding payment-demo \
  --project=driftscribe-hack-2026 \
  --region=asia-northeast1 \
  --member=serviceAccount:rollback-agent-sa@driftscribe-hack-2026.iam.gserviceaccount.com \
  --role=roles/run.developer
```

The same SA does **not** appear in the project-level IAM policy for `roles/run.developer`. There is no application code path through which the worker would attempt to roll back another service — `TARGET_SERVICE` is sourced from env at boot and the request schema (`ProposeRequest` / `ExecuteRequest` with `extra="forbid"`) rejects any caller-supplied service field, verified by `workers/rollback/tests/test_rollback.py::test_propose_rejects_extra_field`. The IAM scoping is the defense-in-depth backstop: even if a bug in the worker's policy check were exploited to swap the service name, Cloud Run's admin API would return 403 because the SA lacks `roles/run.developer` on any service other than `payment-demo`.

## Acknowledged constraints

- **Firestore lacks collection-scope IAM, but offers database-level IAM.** The coordinator and the rollback worker need `roles/datastore.user` to reach `approvals/` and `sessions/`; `tofu-apply-sa` needs it to reach the isolated plan-approval records. Phase C5f isolates `plan_approvals` into a **separate named Firestore database `plan-approvals`** (same `asia-northeast1` region as `(default)`) and uses **CONDITIONED** `roles/datastore.user` to fence the three SAs apart at the database level: coordinator + rollback-agent-sa conditioned to `(default)`, `tofu-apply-sa` conditioned to `plan-approvals` (`resource.name == "projects/driftscribe-hack-2026/databases/<DB>"`). This is enforced by the REST/client libraries (the Cloud Console does **not** honour IAM conditions). Within a database we still rely on the application's transactional writes plus the Layer 0 tool registry (the coordinator's ADK agent has no general-purpose Firestore tool) to bound the blast radius across collections. Worker env `PLAN_APPROVALS_DB` selects the DB (empty ⇒ `(default)` for back-compat).
- **Eventarc-to-Cloud-Run auth is verified by the `/eventarc` application handler**, not by Cloud Run's IAM check. The coordinator runs `--allow-unauthenticated` at the Cloud Run admission layer (Phase 11.1, so /chat and /recheck can take the operator-token guard at Layer A), which means Cloud Run forwards the Eventarc-minted ID token without ever IAM-checking it. The handler at `agent/main.py` `/eventarc` verifies the Google-signed bearer via `google.oauth2.id_token.verify_oauth2_token` against `EVENTARC_AUDIENCE` and rejects anything whose `email` claim is not `eventarc-trigger-sa@<project>.iam.gserviceaccount.com`. **`EVENTARC_AUDIENCE` is the coordinator URL PLUS the `/eventarc` path** (`<coord_url>/eventarc`, stamped by the post-deploy step in `infra/cloudbuild.yaml`): Eventarc delivers via a Pub/Sub push subscription whose OIDC token audience is the push endpoint = service URL + the trigger's `--destination-run-path`, and `verify_oauth2_token` exact-matches `aud`. This is deliberately DIFFERENT from the coordinator→worker call path, which uses the bare ROOT service URL as audience (Cloud Run's platform validates worker tokens against the root URL — see `tests/unit/test_worker_client.py`); the `/eventarc` token is app-verified, not platform-verified, so it carries the path-suffixed audience Pub/Sub actually mints. (A bare-URL `EVENTARC_AUDIENCE` makes every real delivery 401 — the trigger stays ACTIVE and silently drops events. Guarded by `tests/integration/test_eventarc_setup.py`.) The `roles/run.invoker` grant on the trigger SA exists because Eventarc validates the SA at trigger-create time, not because it gates the request path. This intentionally bypasses Layer A (the operator token) and Layer B (audience-bound caller-email allowlist for worker-call auth) because the audit-log source IS the authorization — the SA email claim and Google's signature on the ID token together are what tie the event back to Eventarc.

## Phase 11.7 IAM trim

The coordinator rewrite (Phase 11.7) is the point at which the legacy direct-GCP and direct-GitHub mutation surfaces on `driftscribe-agent@…` are *operationally* removed. The plan only adds the new per-worker `roles/run.invoker` bindings; the removals of older project-wide grants are done by hand via the commands below, because a `gcloud builds submit` cannot itself remove bindings it didn't add.

**Phase 11.8 automation:** The per-worker `roles/run.invoker` grants and the rollback worker's resource-scoped `roles/run.developer` on `payment-demo` are now applied by `infra/scripts/setup_secrets.sh` automatically — gated on each service's existence, so the script is safe to run before the first build (the grants are no-ops) and re-run after the first build (the grants apply). The authoritative worker set the loop covers lives in that script and is currently **seven** services (the drift trio + shared notifier, the two upgrade workers, and the read-only `driftscribe-infra-reader` added in Phase infra-iac B). The four-worker `for` loop shown below is the original Phase 11.7 illustration and is **not** the current list.

**Phase 13 automation:** The `roles/run.viewer` *removal* on the coordinator is also done by `setup_secrets.sh` automatically — the script no longer adds the grant on fresh deploys AND issues an idempotent `remove-iam-policy-binding ... || true` so any pre-Phase-13 deploy cleans up on re-run. The gcloud command in step 2 below is kept as a manual-fallback reference but is no longer the recommended path.

**Idempotency note:** these are `|| true`-suffixed in production runbooks so re-running them on an already-trimmed deployment is a no-op. The negative-space audit script in `infra/scripts/audit_iam.sh` (Phase 11.9+, TODO) is the canonical check that the trim has actually happened.

```bash
PROJECT=driftscribe-hack-2026
REGION=asia-northeast1

# 1. Grant the coordinator roles/run.invoker on each of the four worker
#    services (per-service binding, not project-wide). After 11.7 every
#    coordinator → worker call mints an audience-bound ID token; the
#    receiving worker's IAM check is what allows the call to land. The
#    workers also enforce in-app email allowlists (Layer 3), so this is
#    belt-and-suspenders.
for worker in driftscribe-reader driftscribe-docs driftscribe-rollback driftscribe-notifier; do
  gcloud run services add-iam-policy-binding ${worker} \
    --project=${PROJECT} \
    --region=${REGION} \
    --member=serviceAccount:driftscribe-agent@${PROJECT}.iam.gserviceaccount.com \
    --role=roles/run.invoker
done

# 2. Remove legacy project-wide grants the coordinator no longer needs.
#    All four delegations are now in place — these bindings would just
#    be unused privilege.
#
#    NOTE (Phase 13): this is now done by setup_secrets.sh automatically
#    — the script no longer adds run.viewer to the coordinator AND issues
#    a `|| true`-suffixed remove-iam-policy-binding so pre-Phase-13
#    deploys clean up on re-run. The command below is kept as a manual
#    fallback for operators who can't (or won't) re-run setup_secrets.sh.
gcloud projects remove-iam-policy-binding ${PROJECT} \
  --member=serviceAccount:driftscribe-agent@${PROJECT}.iam.gserviceaccount.com \
  --role=roles/run.viewer || true

# (No other project-wide GCP-mutation roles to remove — the coordinator
#  never had roles/run.developer in the first place; that was always
#  delegated to the rollback worker.)
```

**Critical:** do NOT remove `roles/datastore.user` from the coordinator. The coordinator writes to the `sessions/` collection for the state store and READS `approvals/` to render the operator-facing approval page. Removing this grant would break `/recheck` idempotency. The `pending → denied` flip itself moved to the rollback worker in Phase 11.9 (see `worker_client.call_deny` in `agent/main.py` → `/deny` in `workers/rollback/main.py` — the rollback worker is the only service that holds the HMAC key needed to verify the deny token), so the deny path no longer requires Firestore writes from the coordinator. Sessions + reads remain the load-bearing reasons.

## Phase 11.9 carry-overs from Codex 11.7 review

Codex review of Phase 11 surfaced two Layer 1 weaknesses that the 11.9
follow-up commit chose to document rather than close immediately. The
first (`roles/run.viewer` on the coordinator) was closed in Phase 13;
the second remains open:

- **`github-pat` blast radius depends on operator hygiene.** The
  coordinator's `search_recent_prs_tool` calls GitHub's PR list/read
  APIs, and **since Phase C5e the coordinator also legitimately merges
  approved IaC PRs** — so the github-pat is no longer read-only. The
  correctly-scoped fine-grained PAT (single repo `adi-prasetyo/driftscribe`
  only; `Contents: Read and write` for the merge endpoint, `Pull
  requests: Read`, `Checks: Read` (check-runs only) — nothing
  else, no admin, no second repo) confines the blast radius to exactly
  what C5e exercises. But Secret Manager does not enforce the PAT's
  scope, and the LIVE `github-pat` is currently an **over-scoped classic
  PAT** (repo/workflow/admin/push on both `adi-prasetyo/driftscribe` and
  `driftscribe-e2e-target`) — so the effective capability vastly exceeds
  the application's needs until Phase C5f rotates it to the fine-grained
  PAT above. Phase 13 should follow up with an audit script or
  startup-time scope check.

The application-level Layer 0 (tool registry, see
[`multi-agent-design.md`](./multi-agent-design.md) §4) means the LLM
cannot exercise the remaining `github-pat` weakness through normal
control flow — the only tools that can read Cloud Run or call GitHub
at all are `read_live_env_tool` (delegates to Reader worker),
`search_recent_prs_tool` (PR list/read), and the C5e PR-merge path
(merges only an already-approved IaC PR on `adi-prasetyo/driftscribe`).
The Layer 1 weakening is what would manifest if the coordinator's own
code were exploited or if the SA were directly impersonated — and it
is materially larger while the LIVE secret remains an over-scoped
classic PAT pending the C5f rotation.

## Cross-references

- Multi-agent topology and auth layers: [`multi-agent-design.md`](./multi-agent-design.md)
- Architecture diagram (trigger fan-in + layered safety, two stacked SVGs): [`architecture.html`](./architecture.html)
- Phase 17 implementation plan (multi-agent framework + Developer Knowledge MCP): `docs/plans/2026-05-19-driftscribe-phase17-framework-mcp.md`
- Original Phase 11 multi-agent plan (historical): `docs/plans/2026-05-19-driftscribe-v3-multi-agent.md`
- Workload registry + UPGRADE_TARGET_REGISTRY: `agent/workloads/registry.py`
- Upgrade-side workers: `workers/upgrade_reader/main.py`, `workers/upgrade_docs/main.py`
- Post-LLM validator (upgrade-docs Layer 3): `workers/upgrade_docs/validator.py`
- Deploy-pin guard (registry ⇄ worker regex agreement): `tests/integration/test_upgrade_deploy_pin.py`
- Cloud Run inter-service auth proof: spike 11.0 (`spikes/cloud_run_auth/`, retired 2026-05-30 — the mechanism now ships in the production workers; see git history for the original spike + README)
