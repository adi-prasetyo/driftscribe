# Drift workload manifest (Phase 17.A.1).
#
# Symbolic names only. The coordinator's central registry
# (agent/workloads/registry.py) resolves each name to a real callable
# or WorkerEndpoint. Real URLs / secrets / repos / audiences NEVER
# appear in this file — they would be authority leaks. Flipping a
# value here can choose from the allowlist but cannot widen capability.
#
# Anything referenced here that isn't in TOOL_REGISTRY / WORKER_REGISTRY
# / ACTION_REGISTRY fails coordinator boot loudly.

name: drift
# Operator-facing identity (Phase 17.G crew rename). The symbolic ``name``
# above is FROZEN; ``display_name`` is the agent's human identity and
# ``descriptor`` its domain subtitle (rendered "Anchor — Cloud Run config").
# "Anchor" = keeps the live service true to its contract (the positive of
# "drift"). This is the ONLY workload with a live autonomous trigger
# (Eventarc → /eventarc, hardcoded workload=drift); see
# ``AUTONOMOUS_TRIGGER_WORKLOADS`` in agent/main.py.
display_name: Anchor
descriptor: Cloud Run config
description: >
  Detect configuration drift between a Cloud Run service's live env
  vars and the team's declared ops-contract.yaml. Propose docs PRs
  (for sanctioned changes) or HITL rollbacks (for unsanctioned drift
  on allow_manual_change=false vars). Event-triggered via Eventarc —
  it runs when the service changes, not on a polling loop.

# Relative to this workload's directory (workloads/drift/).
system_prompt_file: system_prompt.md

# Phase 17.C.4 (Option A): the /chat surface uses a separate prompt
# from /recheck. Drift's chat prompt was historically the
# coordinator-wide ``SYSTEM_PROMPT_CHAT`` constant in
# ``agent/adk_agent.py``; the 17.C.4 move preserved it byte-for-byte, and
# it has since evolved intentionally (e.g. the 2026-06-28 sibling-crew
# routing block). Still byte-pinned by
# ``tests/unit/test_drift_workload_loads.py``.
chat_system_prompt_file: chat_system_prompt.md

# Path to the workload's declarative contract, relative to this
# workload's directory. Phase 17.A.2 copied demo/ops-contract.yaml into
# this directory so prompt + contract + tool inventory all live under
# workloads/drift/. The repo-root copy (demo/ops-contract.yaml) is kept
# for the demo scripts, cloudbuild deploy, and the checker CLI; a guard
# test (tests/unit/test_drift_workload_loads.py::test_drift_contract_yaml_matches_demo_copy)
# pins the two copies byte-equal so they can't silently diverge.
contract_file: contract.yaml

# Symbolic tool names — resolved to callables via TOOL_REGISTRY.
# This list is the workload-scoped subset of the global registry; the
# coordinator hands the LLM exactly these tools and nothing else.
enabled_tool_names:
  - drift_read_live_env
  - drift_patch_docs
  - drift_propose_rollback
  - notify
  - load_contract
  - search_recent_prs
  # Developer Knowledge MCP (Phase 17.B.3). Drift uses these to ground
  # docs PR body wording in authoritative Cloud Run env-variable docs.
  - search_developer_docs
  - retrieve_developer_doc
  # Cross-crew "team memory" — read what other crews discussed (read-only).
  - read_conversations

# Symbolic worker names — resolved to WorkerEndpoint via WORKER_REGISTRY.
# Each maps to a Cloud Run worker the coordinator may call (audience-
# bound ID token, hardcoded endpoint path).
worker_names:
  - drift_reader
  - drift_docs
  - drift_rollback
  - notifier

# Tells the coordinator which observation type this workload ingests.
# Constrained literal — adding a new kind requires a schema change.
observation_kind: cloud_run_env

# Symbolic action names — resolved to ActionSpec via ACTION_REGISTRY.
# Gates which DecisionAction values the validator accepts.
action_names:
  - docs_pr
  - drift_issue
  - escalation
  - no_op
  - rollback
